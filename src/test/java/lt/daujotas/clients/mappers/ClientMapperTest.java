package lt.daujotas.clients.mappers;

import lombok.var;
import lt.daujotas.Users.clientData.ClientData;
import org.junit.jupiter.api.Test;

import java.sql.Date;
import java.util.UUID;

import static org.junit.jupiter.api.Assertions.*;

class ClientMapperTest {

//    @Test
//   public void toDto_wheGivenValidClient_mapsToDto() {
//        var clientMapper=new ClientMapper();
//        var clientUUID= UUID.randomUUID();
//        ClientData clientData = new ClientData(123L, "food", "dviratis", "nera", "lordas", "ilgas kelias iki namu", "325645789", "password", "pasword", "email@gmail.ccc", new Date(2023 - 10 - 10),"JOnas", clientUUID);
//     var dto=  clientMapper.toDto(clientData);
//     assertEquals(clientData.getUserName(), dto.getUserName());
//    }

    @Test
    void fromDto() {
    }

    @Test
    void testFromDto() {
    }
}